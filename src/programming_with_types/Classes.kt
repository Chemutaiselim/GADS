package programming_with_types

class Classes {
    /*
    Kotlin is object oriented
    Hence it supports classes
    classes in kotlin are public by default, they are also final, stop somebody from
    inheriting them,by default
    Kotlin accepts abstract classes, uses modifiers, supports sealed classes, constructors
    and data classes

     */
    /*

     */
}

